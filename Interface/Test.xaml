<Page x:Class="ProbSciANA.Interface.Test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Test">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Tester les méthodes" FontSize="20" Margin="0,0,0,20"/>
        <Button Style="{StaticResource MenuButtonStyle}" Content="Btn1" Width="200" Margin="5" Click="Btn1"/>
        <Button Style="{StaticResource MenuButtonStyle}" Content="Btn2" Width="200" Margin="5" Click="Btn2"/>
        <Button Style="{StaticResource MenuButtonStyle}" Content="Btn3" Width="200" Margin="5" Click="Btn3"/>
        <TextBlock Text="Le temps de trajet est de : {Binding TempsTrajet}" 
                   FontSize="18" 
                   Margin="10"/>

        <Button Style="{StaticResource MenuButtonStyle}" Content="Retour" Width="200" HorizontalAlignment="Center"
            Click="BtnRetour_Click"/>
        <Button Style="{StaticResource MenuButtonStyle}" Content="Retour à l'accueil" Width="200" Height="35" Margin="10"
        Click="BtnRetourAccueil_Click" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
    </StackPanel>
</Page>